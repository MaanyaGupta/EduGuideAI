Place `College_data.csv` here for deployment, or set `COLLEGE_DATA_PATH` in `.env`.

During local development this project also auto-detects the provided root dataset at `../../data/College_data.csv`.
